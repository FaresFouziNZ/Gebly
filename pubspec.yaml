name: gebly
description: "Gebly mobile application"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.2
  flutter_otp_text_field: ^1.5.1+1
  go_router: ^15.1.2
  hooks_riverpod: ^2.6.1
  supabase_flutter: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/